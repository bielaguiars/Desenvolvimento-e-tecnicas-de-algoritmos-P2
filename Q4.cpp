#include <stdio.h>
#include <math.h>

int main() {
    long long int ttl_grs; 
    ttl_grs = pow(2, 64) - 1;

    printf("O total de grãos de trigo em um tabuleiro de xadrez é: %lld\n", ttl_grs);

    return 0;
}

